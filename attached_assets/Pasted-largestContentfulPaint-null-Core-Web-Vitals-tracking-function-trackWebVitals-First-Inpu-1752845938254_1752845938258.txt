largestContentfulPaint = null; // Core Web Vitals tracking function trackWebVitals() { // First Input Delay (FID) function trackFID() { new PerformanceObserver((entryList) => { for (const entry of entryList.getEntries()) { firstInputDelay = entry.processingStart - entry.startTime; gtag('event', 'timing_complete', { name: 'first_input_delay', value: Math.round(firstInputDelay) }); break; } }).observe({type: 'first-input', buffered: true}); } // Largest Contentful Paint (LCP) function trackLCP() { new PerformanceObserver((entryList) => { const entries = entryList.getEntries(); const lastEntry = entries[entries.length - 1]; largestContentfulPaint = lastEntry.startTime; gtag('event', 'timing_complete', { name: 'largest_contentful_paint', value: Math.round(largestContentfulPaint) }); }).observe({type: 'largest-contentful-paint', buffered: true}); } // Cumulative Layout Shift (CLS) function trackCLS() { let clsValue = 0; new PerformanceObserver((entryList) => { for (const entry of entryList.getEntries()) { if (!entry.hadRecentInput) { clsValue += entry.value; } } gtag('event', 'timing_complete', { name: 'cumulative_layout_shift', value: Math.round(clsValue * 1000) }); }).observe({type: 'layout-shift', buffered: true}); } if ('PerformanceObserver' in window) { trackFID(); trackLCP(); trackCLS(); } } window.addEventListener('load', function() { const loadTime = performance.now() - startTime; console.log(`🚀 Page fully loaded in ${Math.round(loadTime)}ms`); // Track page load time gtag('event', 'timing_complete', { name: 'page_load_time', value: Math.round(loadTime) }); // Track performance metrics if ('performance' in window && 'getEntriesByType' in performance) { const navigation = performance.getEntriesByType('navigation')[0]; if (navigation) { gtag('event', 'timing_complete', { name: 'dns_lookup_time', value: Math.round(navigation.domainLookupEnd - navigation.domainLookupStart) }); gtag('event', 'timing_complete', { name: 'server_response_time', value: Math.round(navigation.responseStart - navigation.requestStart) }); gtag('event', 'timing_complete', { name: 'dom_content_loaded', value: Math.round(navigation.domContentLoadedEventEnd - navigation.domContentLoadedEventStart) }); } } // Performance warnings if (loadTime > 3000) { console.warn('⚠️ Page load time is slow:', Math.round(loadTime), 'ms'); gtag('event', 'performance_issue', { issue_type: 'slow_page_load', value: Math.round(loadTime) }); } // Track Core Web Vitals trackWebVitals(); }); // Error tracking window.addEventListener('error', function(e) { gtag('event', 'exception', { description: e.message, fatal: false, file: e.filename, line: e.lineno }); }); // Unhandled promise rejection tracking window.addEventListener('unhandledrejection', function(e) { gtag('event', 'exception', { description: 'Unhandled Promise Rejection: ' + e.reason, fatal: false }); }); // Page visibility tracking document.addEventListener('visibilitychange', function() { gtag('event', 'page_view', { page_title: document.title, page_location: window.location.href, visibility_state: document.visibilityState }); }); })();